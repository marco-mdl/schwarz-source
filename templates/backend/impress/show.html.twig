{% extends 'base.html.twig' %}

{% block title %}Impress{% endblock %}

{% block body %}
    <h1>Impress</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ impress.id }}</td>
            </tr>
            <tr>
                <th>Language</th>
                <td>{{ impress.language }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ impress.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('backend_impress_index') }}">back to list</a>

    <a href="{{ path('backend_impress_edit', {'id': impress.id}) }}">edit</a>

    {{ include('backend/impress/_delete_form.html.twig') }}
{% endblock %}
